# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.5.0] - 2026-09-08

### 新增

- `port:端口号` 直达检测：注册 `port:` URI 协议后，Win+R 运行窗口、浏览器地址栏、开始菜单搜索直接输入 `port:8001` 即可弹出检测窗口，显示占用进程名、PID、文件路径，支持按 `K` 一键释放端口
- 命令行直达检测：CMD 中输入 `port 8001`（或冒号写法 `port :8001`）直接查询端口，显示占用详情并支持 K 键释放；`port port:8001` 写法同样支持。注意 CMD 本身无法执行 `port:8001`（Windows 判定语法错误），该写法保留给 Win+R / 浏览器 / 开始菜单搜索
- 直达检测退出码：`0` = 端口空闲，`1` = 被占用，`2` = 端口号无效，便于脚本判断
- 协议随 PATH 一并自动安装（双击首次运行 / `--install-path`），`--uninstall-path` 一并注销；每次交互启动自动刷新注册，exe 移动位置后仍可用

## [0.4.0] - 2026-08-29

### 新增

- 主菜单快捷检测：直接输入端口号（2 位以上，如 `8001`）立即查询该端口，无需先进入菜单 [1]
- 单端口检测一键释放：检测结果后按 `K` 直接结束占用进程并释放端口（自动跳过 svchost 等系统关键进程以防误杀）

### 修复

- CI 创建 Release 时 403 权限错误：build.yml 显式声明 `permissions: contents: write`

## [0.3.0] - 2026-08-29

### 新增

- `port` 命令：双击运行一次即自动将程序目录加入用户 PATH（无需管理员权限），之后任意 CMD 窗口输入 `port` 即可使用；已包含防重复安装与卸载支持（`--install-path` / `--uninstall-path`）
- 可执行文件更名为 `port.exe`（原 `port_checker.exe`），使命令名与功能一致

## [0.2.0] - 2026-08-29

### 新增

- 命令行模式：`-c` 检测端口、`-l` 列出端口、`-f` 进程反查、`-a` 自动找可用端口、`-s` 范围扫描，支持脚本调用与可编程退出码
- `--version` / `--help` 参数
- CI 自动构建（GitHub Actions）：推送 `v*` 标签自动编译并将 exe 发布到 Release
- 冒烟测试脚本 `tests/run_tests.ps1`
- 版本信息嵌入 exe（右键属性 → 详细信息可见）
- 英文文档 [README_EN.md](README_EN.md)
- 贡献指南 [CONTRIBUTING.md](CONTRIBUTING.md) 与 Issue / PR 模板

## [0.1.0] - 2026-08-29

### 新增

- 首个版本，交互式菜单包含 9 大功能：
  - 单端口检测（TCP/UDP，进程名、PID、文件路径、常见端口说明）
  - 全部监听端口列表（TCP / UDP 过滤）
  - 端口范围扫描
  - 一键杀死进程（带二次确认）
  - 进程详情（内存、启动时间、文件版本、资源管理器定位文件）
  - 端口连接详情（远程地址、连接状态统计）
  - 进程反查端口
  - 实时监控（状态变化高亮提醒 + 响铃）
  - 自动找可用端口
- 静态链接单文件 exe（约 1 MB），零依赖
